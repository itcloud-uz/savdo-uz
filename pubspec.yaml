name: savdo_uz
description: A modern POS and inventory management Flutter application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase (So'nggi barqaror versiyalar)
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.1
  cloud_firestore: ^6.0.0
  firebase_storage: ^13.0.0 # Yangilandi
  flutter_barcode_scanner: ^2.0.0
  simple_animations: ^5.0.2 

  # State Management
  provider: ^6.1.2

  # Machine Learning (YANGILANGAN VA MOSLASHTIRILGAN VERSIYALAR)
  camera: ^0.11.2
  camera_windows: ^0.2.6+2
  google_mlkit_face_detection: ^0.13.1
  tflite_flutter: ^0.11.0
  image: ^4.1.7
  google_mlkit_commons: ^0.11.0

  # UI & Utility
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  mobile_scanner: ^7.0.1 # Asosiy skaner sifatida qoldirildi
  intl: ^0.20.2
  fl_chart: ^1.0.0
  path_provider: ^2.1.2
  image_picker: ^1.1.2
  pdf: ^3.11.0
  printing: ^5.12.0
  cached_network_image: ^3.3.1
  qr_flutter: ^4.1.0
  
  # flutter_barcode_scanner olib tashlandi, chunki mobile_scanner ishlatilmoqda

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/ml/
    - assets/ml/mobile_face_net.tflite
    - assets/images/kirish_orqafon.jpg
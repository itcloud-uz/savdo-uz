name: savdo_uz
description: A modern POS and inventory management Flutter application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase (So'nggi barqaror versiyalar)
  firebase_core: ^2.25.0
  firebase_auth: ^4.17.8
  cloud_firestore: ^4.15.8
  firebase_storage: ^11.6.9 # Yangilandi
  flutter_barcode_scanner: ^2.0.0

  # State Management
  provider: ^6.1.2

  # Machine Learning (YANGILANGAN VA MOSLASHTIRILGAN VERSIYALAR)
  camera: ^0.10.5+9
  google_mlkit_face_detection: ^0.11.0
  tflite_flutter: ^0.10.4
  image: ^4.1.7
  google_mlkit_commons: ^0.7.0

  # UI & Utility
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  mobile_scanner: ^3.5.7 # Asosiy skaner sifatida qoldirildi
  intl: ^0.19.0
  fl_chart: ^0.67.0
  path_provider: ^2.1.2
  image_picker: ^1.1.2
  pdf: ^3.11.0
  printing: ^5.12.0
  cached_network_image: ^3.3.1
  qr_flutter: ^4.1.0
  
  # flutter_barcode_scanner olib tashlandi, chunki mobile_scanner ishlatilmoqda

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/ml/mobile_face_net.tflite